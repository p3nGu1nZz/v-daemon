# v-daemon runtime settings (TOML)

[paths]
run = "run"
logs = "logs"

[pid_files]
daemon = "run/v-daemon.pid"
supervisor = "run/v-daemon-supervisor.pid"
director = "run/v-director.pid"

[logs]
daemon = "logs/daemon.log"
supervisor = "logs/supervisor.log"
director = "logs/director.log"

[runtime]
# Heartbeat interval for the daemon in seconds
heartbeat_interval_seconds = 30
# Prime directive for agents. Agents should read this and align actions to this directive.
directive = "Build a recursive, autonomous development engine (Director) that can summarize, plan, implement small safe patches, verify results, and iterate under automated governance; human interaction is limited to starting and stopping the system and emergency intervention."
# Whether agents may automatically push commits upstream by default (YOLO). If true, agents will push; if false, agents will merge locally but not push.
yolo = true

[director]
# Whether the director may attempt safe, sandboxed execution of small patches (default false)
allow_execute = false
# Branch prefix to use for sandbox branches created by the patcher
sandbox_branch_prefix = "director/sandbox-"
# Run interval for the director's autopilot cycles (seconds)
interval_seconds = 60
