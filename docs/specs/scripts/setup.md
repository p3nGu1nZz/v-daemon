---
name: setup
generated_by: scripts/docs.sh
generated_at: 20260213T162850Z
source: scripts/setup.sh
autogenerated: true
---

## Purpose
#!/usr/bin/env sh
# Minimal setup script to install C++ build dependencies and ninja for parallel builds


## Synopsis / Usage
Usage: scripts/setup.sh [--yes|-y|--check]
Installs required packages (cmake, ninja, a C++ toolchain, git) for common Linux distros.
Use --check to run repository checks only (no installs).
Usage: scripts/setup.sh [--yes|-y|--check]
Installs required packages (cmake, ninja, a C++ toolchain, git) for common Linux distros.
Use --check to run repository checks only (no installs).

## Reference
- Source: scripts/setup.sh
