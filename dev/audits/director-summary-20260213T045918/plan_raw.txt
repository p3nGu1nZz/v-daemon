- Create cmd/daemon skeleton and entrypoint script: Add a minimal daemon command directory with an executable shell entrypoint, basic option parsing, and build/install hooks.
- Implement scripts/lib/process.sh process controller with basic API: Provide start/stop/status/restart functions, PID handling, and graceful shutdown logic used by run.sh and director/worker.
- Add director and worker stub scripts in scripts/lib: Introduce director.sh and worker.sh stubs with config parsing, lifecycle hooks, and placeholder task/heartbeat loops.
- Add minimal MCP client module for GitHub MCP integration: Implement scripts/lib/mcp_client.sh with HTTP client helpers, auth placeholder, and basic API call scaffolding.
- Create CMake target for daemon and test scaffolding: Add out-of-source CMakeLists to produce a 'daemon' target and a 'tests' target wired to CTest for future Catch2 tests.
- Add basic integration tests using scripts/run.sh harness: Add simple smoke tests that invoke the daemon entrypoint, validate PID files, and assert expected log output.
- Add shellcheck and static linting to setup script: Enhance scripts/setup.sh to optionally install shellcheck and run linting as part of scripts/check.sh.
- Add healthcheck and heartbeat stubs for director/worker: Add periodic heartbeat functions and a healthcheck flag so components can signal liveness and readiness.
- Improve logging helper and rotate logs under logs/: Provide a reusable logging helper, ensure logs are written to logs/, and add a basic rotation/truncation policy.
- Add GitHub Actions CI to run scripts/check.sh: Create a workflow that runs scripts/check.sh and basic build/test steps on pushes and pull requests.
- Update README with development, build, and run instructions: Document the out-of-source CMake workflow, scripts/setup.sh usage, and how to run the daemon locally for contributors.
- Add TODO.md mapping tasks to prioritized implementation milestones: Convert high-level TODO items into actionable milestones with owners, estimates, and dependencies for planning.

