- Add scripts/lib/process.sh process controller and PID management: Implement helpers to register/start/stop managed processes and maintain run/*.pid files used by the daemon and supervisor.
- Wire start/stop/status CLI for daemon control: Implement command handling in scripts/run.sh to reliably start, stop, and report daemon and supervisor status.
- Implement supervisor to spawn and monitor the director: Create scripts/lib/supervise.sh to start the director, write a supervisor pidfile, and restart the director on failure.
- Add config/example.yaml with sensible daemon defaults: Provide a sample YAML listing pid paths, log locations, check intervals, and director settings.
- Finalize daemon lifecycle, locking, and PID handling: Ensure daemon acquires locks, writes PID files, and performs clean shutdown that stops the director.
- Create worker agent stub that registers and sends heartbeats: Add scripts/lib/worker.sh which registers with the director, emits periodic heartbeats, and reports readiness/status.
- Extend director with spawn/stop and worker registry: Update scripts/lib/director.sh to manage worker lifecycle, maintain a registry, and assign simple jobs.
- Add end-to-end integration harness for supervisor‑daemon‑workers: Create a dev script to start the full stack, run N workers, and validate audit logs under dev/audits.
- Add top-level CMakeLists and Catch2 test targets: Enable out-of-source builds, fetch Catch2, and add a basic unit test target runnable via ctest/ninja.
- Update README with run/build and troubleshooting guidance: Document example config, supervisor usage, setup steps (Catch2, copilot, docker), and common troubleshooting tips.
- Document local MCP server setup and secure credential guidance: Provide a developer note (docker‑compose example) and instructions for storing GitHub tokens securely in dev.
- Add basic observability with structured logs and metrics stub: Emit timestamped log levels and add a Prometheus metrics endpoint or placeholder for future instrumentation.
