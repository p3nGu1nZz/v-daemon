- Add process controller script under scripts/lib: Implement start/stop/status functions and track run/*.pid files so supervisor and daemon share a single process API.
- Add config/example.yaml with runtime defaults: Provide sample log paths, PID locations, CHECK_INTERVAL, and director/worker settings for reproducible local runs.
- Make copilot CLI usage optional and opt-in: Stop auto-install in setup.sh and guard director_actions.sh to invoke copilot only when explicitly enabled via env or config.
- Document developer quickstart and run instructions: Update README.md to clearly show setup, check, build, run, and config examples for new contributors.
- Add daemon command skeleton in cmd/daemon: Implement configuration parsing, PID handling, lifecycle hooks, and basic logging for a production-ready daemon entrypoint.
- Expose daemon CLI flags start/stop/status and monitor: Provide a consistent CLI for supervising, querying status, and performing clean graceful shutdowns.
- Ensure daemon spawns and supervises director reliably: Wire the daemon to launch the director, monitor health, and restart on failure using the shared process controller.
- Create worker agent stub that registers and heartbeats: Implement a lightweight worker that registers with the director, sends periodic heartbeats, and reports job completion.
- Define director RPC API using JSON-over-UDS or HTTP: Specify registration, heartbeat, job assignment, and status endpoints with a minimal JSON schema for interoperability.
- Add end-to-end integration test harness and script: Automate supervisor→daemon→director→workers startup and verify audits/logs under dev/audits/ to catch regressions.
- Add CMake top-level and test targets for builds: Provide a top-level CMakeLists.txt and test targets to enable native builds and ctest/ninja-based tests.
- Add basic observability with structured logs and health checks: Standardize log format, add per-agent health endpoints, and placeholders for metrics to aid debugging and autoscaling.
-  echo "Copilot CLI not found; installing via https://gh.io/copilot-install"
-  curl -fsSL https://gh.io/copilot-install | bash
-  echo "Copilot CLI already installed"
-  . "$SCRIPT_DIR/director_actions.sh"
-  # run in background (non-blocking) so director heartbeat continues
-  run_autopilot_summary &
