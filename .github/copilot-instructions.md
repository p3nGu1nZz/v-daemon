# Copilot instructions for v-daemon

This file was auto-generated by Copilot CLI: extracted build/test/lint commands, a short architecture summary, and repository conventions.

## Build (extracted)

- Check environment and dependencies: sh scripts/check.sh
- Install common packages (Linux): sh scripts/setup.sh --yes
- Example CMake + Ninja out-of-source build (extracted from scripts/setup.sh):

  mkdir -p build && cd build && cmake -G Ninja .. && ninja -j$(nproc 2>/dev/null || echo 2)

- Notes: scripts/setup.sh ensures cmake, ninja and a C++ toolchain (g++/clang) are available.

## Test (extracted)

- No test runner or test targets were detected in the repository snapshot.
- Typical commands to add/use if tests are added:

  cd build && ctest -j$(nproc)
  # or
  cd build && ninja test

## Lint / Formatting (extracted)

- No linter or formatter detected in this repo snapshot.
- Suggested tooling to add: clang-format (formatting), clang-tidy / cppcheck (static analysis). Consider adding a "lint" CMake target or scripts/lint.sh for CI.

## Run / Runtime helpers (extracted)

- scripts/run.sh — start/stop/status/run-foreground the daemon (Usage: sh scripts/run.sh {start|stop|status|run-foreground})
- scripts/daemon.sh — daemon loop (writes PID to /tmp/v-daemon.pid and logs to scripts/daemon.log)

## Architecture summary (extracted)

- Primary language: C/C++ (gitignore contains common C/C++ build artifacts and setup installs a C++ toolchain).
- Build system: CMake configured to use Ninja (scripts/setup.sh + example build command).
- Runtime: Minimal POSIX shell-based daemon wrapper in scripts/ (daemon.sh + run.sh). The actual C/C++ source tree (e.g., src/) was not present in the repository snapshot.
- Development helpers and installer/checker scripts live under scripts/.

## Repo conventions (extracted / recommended)

- Developer scripts live in scripts/ (setup.sh, check.sh, run.sh, daemon.sh).
- Use an out-of-source build directory (build/) and add build artifacts to .gitignore (already present).
- Prefer CMake + Ninja for local builds and CI; ensure setup.sh and check.sh are executed in CI bootstrapping if needed.
- Runtime conventions: PID file at /tmp/v-daemon.pid; runtime logs at scripts/daemon.log (follow current scripts).

## Quick start (extracted)

1. sh scripts/check.sh
2. sh scripts/setup.sh --yes
3. mkdir -p build && cd build && cmake -G Ninja .. && ninja
4. sh scripts/run.sh start

## Notes / next steps

- Add a README describing expected source layout (e.g., src/, include/, tests/) and CI steps.
- If tests or linters are added, update this file so Copilot and CI can detect and surface the exact commands.

